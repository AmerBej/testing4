function paragraphenMachen() {
    var createDiv = document.createElement("div");
    var inhalt = document.createTextNode("JSON.parse(localStorage.getItem()")
    createDiv.appendChild(inhalt);
}













/*foreach key in localstorage create div, div = document.createElement minimal kleiner als OG txt file, onclick expand*/

/*const keys = Object.keys(localStorage);
for (let key of keys) {
    g = document.createElement("div");
    g.setAttribute("class", "tagebuchEintrag")
    g.setAttribute("id", localStorage[key]);
    g.innerHTML = "localStorage[key].value";
}
*/





/*function divCreation() {
    for (var i = 0; i < localstorage.length; i++) {
        document.createElement("div");
        div.classList.add("tagebuchEintrag");
tagebuchEintrag.onclick(divswitch)
        tagebucheintrag id = localstoragekey
        localstorage.key(i)
        
    }
tagebucheintrag = keyvalue
}*/